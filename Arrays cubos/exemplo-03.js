const palavra = 'abacaxi';

let quantidade = 0;

for (letra of palavra) {
    if (letra === 'a') {
        quantidade++
    }

}

console.log(quantidade);

// exercicio para ver quantas letras a tem na palavra