const nomeEstudantes = ['matheus', 'Bruna', 'Dalvio', 'Maria', 'Rodolfo'];

let indice = 0;

while (indice < nomeEstudantes.length) { //enquanto
    console.log(nomeEstudantes[indice]);
    indice++;
}

// esse while ira se repetir enquanto a variavel indice for menor do que o tamanho do array
