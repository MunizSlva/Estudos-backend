const nomeEstudantes = ['matheus', 'Bruna', 'Dalvio', 'Maria', 'Rodolfo'];

for (let nome of nomeEstudantes) {
    console.log(nome);
}

// criei a variavel nome e entao nomeestudantes ira imprimir o valor dela um de cada vez nessa variavel nome para percorrer todo array
