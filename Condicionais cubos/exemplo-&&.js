const temIngresso = true;
const censura = 16;
const idade = 18;

if (temIngresso === true && idade >= censura) {
    console.log('Pode entrar');
} else {
    console.log('Barrada');
}

// não sei pq a pessoa barrada so sei que ela sera barrada
